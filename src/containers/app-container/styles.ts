import { type SxProps } from "@mui/material";

export const styles = {
  container: {
    display: "flex",
    flexDirection: "column",
    position: "relative",
    minHeight: "100vh",
    maxWidth: "1800px !important",
  } satisfies SxProps,
};
