import { type SxProps } from "@mui/material";

export const styles = {
  wrapper: {
    justifyContent: "center",
    alignContent: "center",
    alignItems: "center",
  } as SxProps,
  container: {
    width: "60vw",
  } as SxProps,
  header: {
    fontWeight: "400",
    fontStyle: "italic",
    fontSize: "150px",
    lineHeight: "94.5px",
  } as SxProps,
  body: {
    fontWeight: "400",
    fontSize: "24px",
    lineHeight: "45px",
    overflow: "wrap",
  } as SxProps,
};
