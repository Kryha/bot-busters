import { type SxProps } from "@mui/material";

export const styles = {
  mainContainer: {
    pt: 30,
    alignItems: "center",
    gap: 2,
  } satisfies SxProps,

  table: {
    mt: "120px",
    mb: 5,
    textAlign: "center",
  } satisfies SxProps,

  actions: {
    gap: 2,
    flexDirection: "column",
    alignItems: "center",
  } satisfies SxProps,
};
