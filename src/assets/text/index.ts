import { auth } from "./auth";
import { general } from "./general";
import { navigation } from "./navigation";
import { lobby } from "./lobby";

export const text = { auth, general, navigation, lobby };
