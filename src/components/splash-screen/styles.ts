import { type SxProps } from "@mui/material";

export const styles = {
  container: {
    position: "fixed",
    inset: 0,
    backgroundColor: "common.white",
    zIndex: 1,
    alignItems: "center",
    justifyContent: "center",
  } satisfies SxProps,
};
