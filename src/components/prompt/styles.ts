import { type SxProps } from "@mui/material";

export const styles = {
  wrapper: {
    width: "100%",
    alignItems: "center",
    backgroundColor: "blueGrey.light",
    p: 2,
    mt: 5,
    mb: 5,
  } satisfies SxProps,
};
