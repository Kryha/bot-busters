import { type SxProps } from "@mui/material";

export const styles = {
  container: {
    minHeight: "100vh",
    display: "flex",
    flexDirection: "row",
    maxWidth: "1800px !important",
    padding: "0px !important",
  } satisfies SxProps,
};
