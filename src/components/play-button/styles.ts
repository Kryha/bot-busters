import { type SxProps } from "@mui/material";

export const styles = {
  playButton: {
    ml: 6,
    width: "200px",
    height: "100px",
    backgroundColor: "transparent",
    border: "none",
    outline: "none",
    cursor: "pointer",
    position: "relative",
  },
} satisfies SxProps;
