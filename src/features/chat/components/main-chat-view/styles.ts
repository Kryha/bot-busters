import { type SxProps } from "@mui/material";

export const styles = {
  section: {
    height: "100vh",
    backgroundColor: "grey.50",
    borderRadius: "10px",
    overflow: "hidden",
    flexGrow: 1,
  } satisfies SxProps,
};
