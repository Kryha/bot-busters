import { type SxProps } from "@mui/material";

export const styles = {
  container: {
    flexDirection: "row",
    alignItems: "center",
    gap: 1,
  },

  avatar: {
    cursor: "pointer",
  } satisfies SxProps,
};
