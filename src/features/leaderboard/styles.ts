import { type SxProps } from "@mui/material";

export const styles = {
  wrapper: { gap: 5, alignItems: "center" },
  container: {
    mt: 2,
    gap: 2,
  },
  text: { textAlign: "center" } satisfies SxProps,
};
