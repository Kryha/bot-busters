import { Typography } from "@mui/material";
import { type FC } from "react";
import { text } from "@/assets/text";

const AnimationLab: FC = () => {
  return <>
  <Typography variant="h5">{text.animationLab.welcome}</Typography>
  </>;
};

export default AnimationLab;
