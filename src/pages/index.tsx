import { Layout, Page } from "@/layouts";
import { Landing } from "@/features";

export default function Home() {
  return (
    <Layout>
      <Page>
        <Landing />
      </Page>
    </Layout>
  );
}
